// --------INITIAL VARIABLES--------
const button = document.querySelector('button');

// Array to hold multiple cards
const myLibrary = [];


function Book() {
    // the constructor
}


function addBookToLibrary() {
    // 1. prompt user (popup dialogue) for information when "add book" button is pushed (title, author, page count, read?(checkbox)). also add confirm and cancel buttons in that popup.

    // 2a. if cancel is pressed, close dialogue and clear info
    // 2b. if submit is pressed, store info as object using the book constructor
    
    // 3. add the new object to the myLibrary[] array.
    
    // 4. create a div with class '.card' to display the data prompt user for information
    
}